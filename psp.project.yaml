apiVersion: psp.dev/v1
kind: PSPScaffoldProject
harness:
  protocol: pre-sdd-harness/v3
  manifest: .psp/harness/harness.manifest.json
template:
  root: templates/workspace
  project: psp.project.yaml
  manifest: .psp/harness/harness.manifest.json
runtime:
  entrypoint: bin/pre-sdd.mjs
  dispatcher: runtime/dispatch.mjs

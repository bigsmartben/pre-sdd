# 维护者原则

本目录属于脚手架源仓库，只允许存在本文件。

- 维护者只修改模板、Python 初始化器、领域 Skill、测试、打包和普通 CI。
- 不在根仓库执行产品或架构交付。
- 保留已有改动，并在操作系统临时目录验证生成工作区。
- 不建立 Manifest、Resolver、Schema、Validator、Profile、Scope、Gate、Hook、缓存、统一报告或移交控制面。
- 工程完成只表示脚手架检查通过，不表示任何业务阶段就绪。

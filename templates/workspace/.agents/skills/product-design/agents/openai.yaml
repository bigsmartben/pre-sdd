interface:
  display_name: "Product Design Domain"
  short_description: "将产品与设计来源转为可执行规范界面并通过机器验收"
  default_prompt: "Use $product-design to route normal implementation to $implement-canonical-ui, return the actual canonical-ui-dev temporary preview when runnable, pause for schema-valid Feedback Packets or the user's explicit request to finish feedback, route only a validator-generated Repair Packet to $repair-canonical-ui after explicit repair authorization, create formal Review Evidence only through canonical-ui-review, and stop at publish-ready until the user separately requests Publish."

interface:
  display_name: "采集 Figma 设计来源"
  short_description: "采集 Figma 节点、导出静态资源并封存参数、截图和哈希证据"
  default_prompt: "Use $capture-figma-design-source only after all Figma writes and scope confirmation are complete. Freeze every candidate visual node into one unique asset, dom-css, dynamic, or ignored strategy, download planned assets into an OS-temporary Acquisition Packet, run the registered controlled Ingest operation, seal the Capture Plan, receipt, assets, and hashes into evidence.json, and return a schema-valid registration packet without editing canonical-ui.ts or inferring product semantics."

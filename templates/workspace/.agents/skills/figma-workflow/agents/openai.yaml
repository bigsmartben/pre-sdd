interface:
  display_name: "Figma 工作流"
  short_description: "统一整理、组件化并封存可登记的 Figma 设计来源证据"
  default_prompt: "Use $figma-workflow to scan and confirm a Figma scope, execute one approved writeback batch, freeze the final nodes, capture and ingest source evidence, and return the registration packet to Product Design without editing Lit code."

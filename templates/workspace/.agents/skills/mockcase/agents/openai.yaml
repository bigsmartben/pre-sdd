interface:
  display_name: "MockCase"
  short_description: "管理独立 MockCase Suite、运行扩展与验证证据"
  default_prompt: "Use $mockcase for the explicitly requested side-path operation. Analyze and Generate stay read-only; run the full scoped workflow without repeated Candidate or Apply confirmation only when the user explicitly requests end-to-end MockCase completion. Never modify Product Design."

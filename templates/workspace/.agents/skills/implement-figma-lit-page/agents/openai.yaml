interface:
  display_name: "实现 Figma Lit 页面"
  short_description: "将 Figma 组件与页面还原为可验证的 Lit 规范界面"
  default_prompt: "Use $implement-figma-lit-page only after the target scope, source version, evidence hashes, assets, component inventory, Figma-to-Lit mappings, and variant coverage are closed. Implement only mapped shared components, render every declared Figma instance through its registered Lit tag and attributes, stop on unresolved abstraction, and run component implementation plus source-parity validation."
